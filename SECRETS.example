# Copy to SECRETS and fill in. SECRETS is gitignored -- never commit it.
# Gemini API key (https://aistudio.google.com/apikey). Free tier covers replay+light live runs.
GEMINI_API_KEY=

# Optional overrides
GEMINI_MODEL=gemini-robotics-er-2-preview
